# Common locations for HTTP redirect servers
# Provides health check and ACME challenge endpoints

location /health {
    access_log off;
    default_type text/plain;
    return 200 "healthy\n";
}

location /.well-known/acme-challenge/ {
    root /var/www/certbot;
}
