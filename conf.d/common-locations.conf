# Common location blocks shared across server configurations
# Included in server {} blocks

location /health {
    access_log off;
    default_type text/plain;
    return 200 "healthy\n";
}

location /.well-known/acme-challenge/ {
    root /var/www/certbot;
}
